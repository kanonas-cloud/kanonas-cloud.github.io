# Kanonas Cloud

Astro-only public marketing site for [kanonas-cloud](https://github.com/kanonas-cloud).

## Development

```sh
npm ci --ignore-scripts
npm test
npm run build
```

The static site keeps public marketing separate from the user, organization, and Shared Auth surfaces.
